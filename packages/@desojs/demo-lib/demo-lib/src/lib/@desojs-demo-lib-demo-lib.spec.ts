import { desojsDemoLibDemoLib } from './@desojs-demo-lib-demo-lib';

describe('desojsDemoLibDemoLib', () => {
  it('should work', () => {
    expect(desojsDemoLibDemoLib()).toEqual('@desojs-demo-lib-demo-lib');
  });
});
